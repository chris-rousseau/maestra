{% extends 'base.html.twig' %}

{% block title %}Dashboard{{ parent() }}
{% endblock %}

{% block body %}
	<div class="container-fluid px-4">
		<h1 class="mt-4 spacing">Bienvenue
			<strong>{{ app.user.firstname}}</strong>
			sur le back-office de Maestra</h1>
		<div class="row justify-content-center spacing">
			<div class="col-lg-4 col-md-12">
				<div class="card text-white bg-info mb-3 users-card">
					<div class="card-body d-flex justify-content-between align-items-center">
						<p class="h3 mb-0">
							<a href="{{ path('admin_user_list') }}" class="text-white text-decoration-none">Nombre d'utilisatrices</a>
						</p>
						<p class="card-text count">{{ countUsers }}</p>
					</div>
				</div>
			</div>
			<div class="col-lg-4 col-md-12">
				<div class="card text-white bg-secondary mb-3 pills-card">
					<div class="card-body d-flex justify-content-between align-items-center">
						<p class="h3 mb-0">
							<a href="{{ path('admin_pill_list') }}" class="text-white text-decoration-none">Nombre de pilules</a>
						</p>
						<p class="card-text count">{{ countPills }}</p>
					</div>
				</div>
			</div>
			<div class="col-lg-4 col-md-12">
				<div class="card text-white bg-success mb-3 reviews-card">
					<div class="card-body d-flex justify-content-between align-items-center">
						<p class="h3 mb-0">
							<a href="{{ path('admin_pill_review_pending') }}" class="text-white text-decoration-none">Avis en attente</a>
						</p>
						<p class="card-text count">{{ reviewsWating }}</p>
					</div>
				</div>
			</div>
		</div>
		<div class="card">
			<div class="card-body">
				<p class="mb-0">Dans le backoffice vous pouvez gérer la totalité des fonctionnalitées du site :
					<strong>valider ou non</strong>
					un avis,
					<strong>ajouter/éditer/supprimer</strong>
					une pilule ou encore passer un utilisateur
					<strong>modérateur ou administrateur</strong>.</p>
			</div>
		</div>
		<div class="card mt-2">
			<div class="card-body">
				<h3>Maintenance</h3>
				<p class="mb-0">
					<a class="btn btn-warning" href="{{ path('admin_delete_image') }}" role="button">
						<i class="far fa-images"></i>
						Supprimer les images des pilules non utilisées</a>
				</p>
			</div>
		</div>
	</div>
{% endblock %}
