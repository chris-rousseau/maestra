{% extends 'base.html.twig' %}

{% block title %}Dashboard{{ parent() }}
{% endblock %}

{% block body %}
	<div class="container-fluid px-4">
		<h1 class="mt-4 spacing">Bienvenue
			<strong>{{ app.user.firstname}}</strong>
			sur le back-office de Maestra</h1>
		<div class="row justify-content-center spacing">
			<div class="col-lg-4 col-md-12">
				<div class="card text-white bg-info mb-3 users-card">
					<div class="card-body d-flex justify-content-between align-items-center">
						<p class="h3 mb-0">Nombre d'utilisatrices</p>
						<p class="card-text count">{{ countUsers }}</p>
					</div>
				</div>
			</div>
			<div class="col-lg-4 col-md-12">
				<div class="card text-white bg-secondary mb-3 pills-card">
					<div class="card-body d-flex justify-content-between align-items-center">
						<p class="h3 mb-0">Nombre de pilules</p>
						<p class="card-text count">{{ countPills }}</p>
					</div>
				</div>
			</div>
			<div class="col-lg-4 col-md-12">
				<div class="card text-white bg-success mb-3 reviews-card">
					<div class="card-body d-flex justify-content-between align-items-center">
						<p class="h3 mb-0">Avis en attente</p>
						<p class="card-text count">{{ reviewsWating }}</p>
					</div>
				</div>
			</div>
		</div>
		<div class="card">
			<div class="card-body">
				<p>Dans le backoffice vous pouvez gérer la totalité des fonctionnalitées du site :
					<strong>valider ou non</strong>
					un avis,
					<strong>ajouter/éditer/supprimer</strong>
					une pilule ou encore passer un utilisateur
					<strong>modérateur ou administrateur</strong>.</p>
			</div>
		</div>
	</div>
{% endblock %}
